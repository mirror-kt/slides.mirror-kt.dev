{
  "name": "example",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build --base \"$(pnpm -c exec echo '$PNPM_PACKAGE_NAME')\" --out \"../../dist/$(pnpm -c exec echo '$PNPM_PACKAGE_NAME')\""
  }
}
